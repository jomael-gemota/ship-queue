openai>=1.82.0
websockets>=14.0
pdfplumber>=0.11.4
motor>=3.7.0
python-dotenv>=1.1.0

# Local OCR for scanned pages. Requires system packages too:
#   Ubuntu:  sudo apt install tesseract-ocr poppler-utils
#   macOS:   brew install tesseract poppler
# OCR degrades gracefully (skips scanned pages) if these are absent.
pytesseract>=0.3.10
pdf2image>=1.17.0
